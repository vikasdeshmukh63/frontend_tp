# Auth service — login, profile, /api/users/* (see auth_service PORT, often 3001 locally).
NEXT_PUBLIC_AUTH_API_URL=http://localhost:3001
# Recruiter service — job openings only (see recruiter_service PORT).
NEXT_PUBLIC_RECRUITER_API_URL=http://localhost:3002